/**
 * The community's name for itself.
 *
 * One constant rather than a string repeated in the rail, the mobile bar and
 * the page metadata — the tagline is the sort of thing that gets rewritten a
 * dozen times before it settles, and it should only ever be rewritten once.
 */

/** Sits under the wordmark. Short enough for a 265px rail. */
export const COMMUNITY_TAGLINE = "Everything immigration, in one place";

/** The fuller line, for anywhere with room to breathe — metadata, empty states. */
export const COMMUNITY_BLURB =
  "Everything you need for your Australian visa — shared by the people going through it.";
